You are working inside my existing Replit project for a hotel booking app called YoRight.
The UI and main pages already exist, but many inner pages are just empty shells. I want you to turn this into a more complete OTA-style app (similar to Booking / Trip.com) and add an admin area.

Please do all of the following, using the existing tech stack of this repo (React/Next.js or whatever you detect). Do not break the current styling or branding; extend it.

⸻

1) Map view on the hotel search results page
	•	Locate the hotel search results page/component (the page that shows hotels after a search from the home screen).
	•	Add a Map view integrated with the list:
	•	On mobile: add a toggle or tabs: List / Map.
	•	On desktop: you can show list on the left and map on the right.
	•	Use any reasonable map solution supported in this stack (e.g. Google Maps / Mapbox / Leaflet).
	•	For each hotel result:
	•	Display a marker on the map using its coordinates.
	•	Clicking a marker highlights or scrolls to the corresponding hotel card in the list.
	•	Clicking a hotel card centers the map on that hotel’s marker.
	•	Make sure current filters (dates, destination, price, etc.) affect both the list and the map.
	•	For now, if real coordinates are missing, generate mock coordinates in the data layer so the map still works.

⸻

2) Make inner pages “real”, not empty shells

Right now, when I click items like Destinations / Explore / “عرض الكل / See all” / destination cards, I often land on blank or almost-empty pages.

I want full flows with mock data instead of empty shells:
	1.	Destinations / Explore
	•	Create a Destinations list page: grid or list of cities with image, country, short description, and a “View hotels” CTA.
	•	Create a Destination detail page:
	•	Hero image, destination name, country.
	•	Short overview text.
	•	Sections: “Top areas”, “Recommended hotels”, “Best time to visit”.
	•	A button to open hotel search pre-filtered for that destination.
	2.	Hotel detail page
	•	Ensure there is a complete hotel detail page that receives an ID/slug.
	•	Add:
	•	Image gallery / carousel
	•	Overview text
	•	Amenities list
	•	Room types with prices (mock data), each with “Select room”
	•	Location map (reuse the map component)
	•	Reviews section (mock reviews)
	•	A main “Book now” / “Continue” button that kicks off the booking flow.
	3.	Booking flow
	•	Implement a multi-step flow after selecting a room:
	•	Step 1: Review selected hotel, room, dates, guests.
	•	Step 2: Guest details form (name, email, phone, special requests).
	•	Step 3: Summary + “Confirm booking” page.
	•	Store bookings in a simple bookings data model (mock data; no real payment or Shopify yet).
	•	After confirm, show a “Booking confirmed” page with booking reference.
	4.	User area
	•	Profile page (you already started one): keep it, but remove any loyalty/points logic if present.
	•	“My bookings” page showing a list of bookings from the mock bookings store.
	•	“Saved / Favorites” page for saved hotels/destinations (mock list).

No route should lead to a blank white page; every clickable card or link must go to a meaningful screen with mock data.

⸻

3) Admin dashboard (/admin)

Create an admin area in this same project with its own route, for example /admin.
It can use a simple hard-coded login guard or a basic check; security is not the priority now.

Admin should include at least:
	1.	Admin Home / Dashboard
	•	KPIs based on mock data:
	•	Total searches (mock number)
	•	Total bookings
	•	Total revenue
	•	Top destinations
	•	Top suppliers
	•	Use simple charts or summary cards.
	2.	Bookings management
	•	A table listing bookings from the same bookings data model used by the public app:
	•	Columns: booking ID, customer name, email, dates, destination, hotel, supplier, status, total amount.
	•	Ability to filter by date range, destination, supplier, status.
	•	Ability to change status (e.g. Pending / Confirmed / Cancelled) purely in the mock data.
	3.	Suppliers / API Providers management
	•	A page that lists hotel suppliers (API providers) such as: RateHawk, Hotelbeds, Expedia, etc.
	•	For each supplier, store:
	•	Name
	•	Base API URL
	•	API key / credentials (stored server-side only – never exposed in client code)
	•	Status (enabled/disabled)
	•	Default markup/commission percentage.
	•	Provide a form to add/edit/remove suppliers.
	•	For now, use a config file or server-side store for this; actual secrets can be mocked or read from env variables.

⸻

4) API integration layer (structure only, using mock data)

Prepare the code structure so that later I can plug in real APIs from RateHawk, Hotelbeds, etc.
	•	Create a provider abstraction:
	•	A common TypeScript interface or type for hotel search results (id, name, destination, coordinates, price, currency, rating, images, cancellation policy, etc.).
	•	A function like searchHotels(query) in a central service module.
	•	Inside that service:
	•	Implement provider modules, e.g. ratehawkProvider, hotelbedsProvider, etc.
	•	Right now they should return mock data but already follow the correct unified interface.
	•	searchHotels(query) should:
	•	Call all enabled providers (from the admin suppliers config).
	•	Merge and normalize results into one unified list.
	•	Return that to the search results page (which feeds both list and map).
	•	In the admin suppliers page:
	•	Show which providers are enabled.
	•	Indicate where API keys and base URLs will be configured later (environment variables/config).

No real network calls are required yet; just design the architecture and interfaces as if they will call real APIs soon.

⸻

5) General guidelines
	•	Keep the existing YoRight branding, colors, and typography.
	•	Focus on mobile-first layout but make sure everything is responsive on larger screens.
	•	Reuse components (header, footer, bottom nav, hotel cards, destination cards, map component) across pages.
	•	Ensure routing/navigation works end-to-end: no dead links or blank pages.

Implement all of this directly in this Replit project, updating whatever files are necessary.