You are a Staff+ Native Mobile Architect and Delivery Lead. Generate a single monorepo that ships “YoRight” — a world-class, fully NATIVE travel app (Arabic default, English optional) that outclasses Almosafer/Trip.com. Do NOT ask questions. Pick sane defaults. Produce working code, seeds, tests, CI/CD, docs, dashboards, store assets, and a real booking flow. Use Google Maps ONLY. Saudi market first: SAR, VAT, Apple Pay, mada. Monorepo must run on Replit for gateway and Android; iOS builds require macOS.

OBJECTIVE AND SCOPE EXPANSION
• Build native iOS (Swift 5.10+, SwiftUI, Combine, iOS 16+) and native Android (Kotlin 2.0+, Jetpack Compose, Hilt, Coroutines/Flows, Android 8+).
• Build a backend gateway (Node 20, NestJS + Fastify, PostgreSQL, Redis optional) as a secure aggregator for suppliers and payments.
• GraphQL primary for apps; REST compatibility layer for admin tools. Generate Swift/Kotlin clients from schema.
• Include complete DevOps: CI/CD, release trains, Sentry, Firebase, synthetic monitors, runbooks, and SLIs/SLOs.
• Include brand-grade UX, Arabic-first with perfect RTL, Arabic-Indic numerals, Hijri support, prayer times card (toggle).
• Include a data playbook, licensing notes, and a CMS-light to override content (AR/EN) safely.
• Include mock mode when secrets are missing so the demo works end-to-end.

MONOREPO LAYOUT
/ apps/ios
/ apps/android
/ apps/gateway
/ apps/admin (lightweight Next.js admin panel served by the gateway)
/ packages/contracts (GraphQL schema, OpenAPI shim, generated Swift/Kotlin clients)
/ packages/design-tokens (brand tokens JSON → Swift/Kotlin generators for themes)
/ packages/quality (lint/format configs, shared test utils, danger rules)
/ packages/ml (ranking heuristics, quality scorers, simple on-device models)
/ tools/fastlane (lanes for build/test/beta/release/screenshots)
/ tools/scripts (bootstrap, seed, smoke, screenshot, changelog, backfill, synthetic-monitors)
/ docs (ADR log, architecture, sequences, security, privacy, MASVS map, PDPL checklist, data-playbook, disaster-recovery, SRE runbooks, growth playbook)
/ store (App Store/Play metadata AR/EN, privacy labels, screenshot manifests, keywords per market)
/ .replit and replit.nix to run gateway and Android build targets on Replit

ARCHITECTURE FOUNDATIONS
• Clean Architecture per feature: Presentation → Domain → Data.
• CQRS + read-models to make SRP/PDP fast; write side publishes domain events.
• Event bus inside the gateway to fan-out analytics, cache invalidations, alerts, email receipts, and payment reconciliations.
• Multi-tenant and white-label: theme tokens, logo, primary color, localized copy blocks, feature flags by tenant/country.
• Edge precompute: materialize destination popularity, seasonal scores, and reference price baselines.

PLATFORM CHOICES AND NON-NEGOTIABLES
• Google Maps SDK only; Places Autocomplete; cluster manager; draggable radius; neighborhood polygons; price pins.
• Payments via Tap: intents, 3DS, Apple Pay, mada, cards; webhooks with signature verification and idempotency.
• Arabic is default; Asia/Riyadh timezone; Hijri calendar tags optional; Arabic-Indic numerals in AR locales.
• PCI scope minimized (SAQ A) using provider sheets; never store PANs; no payment screens offline.
• Certificate pinning with backup keys; jailbreak/root heuristics; device binding toggle for high-risk actions.

NATIVE EXPERIENCES THAT DIFFERENTIATE
• Real-time Price Watch on SRP via WebSocket/SSE; tiles update live when suppliers move.
• Trip Companions: invite link, shared itinerary view, per-traveler forms, split contact details at checkout.
• Split and Partial Payments: single payer or multiple payers; partial pre-auth and capture after supplier confirmation; partial refunds per line item with VAT logic.
• Installments adapter (toggle): wire stubs for Tabby/Tamara with consistent UX and analytics, no duplication.
• Smart Cancellations: policy timeline visualization, grace periods, cancel eligibility state machine.
• City Boards: editorial tiles (neighborhoods, “best months”, family/beach/shopping).
• Prayer Times and Qibla card (toggleable): city-aware, discreet AR-only presentation.
• One-Tap Rebook and “Same Room Again”: clone previous booking, auto-match equivalent rate plan, suggest closest dates with lowest price window.
• Offline-first browsing: SRP/PDP cached snapshots; bookings and receipts readable offline; mutation queues for non-financial actions.

MAPS AND GEO DEPTH
• Isolines radius filter approximating time-to-center using POI density and road expectations.
• Neighborhood polygons in read-model; combine with hotel density heatmap; price buckets with collision-aware labels at 60fps.
• Draw-your-area search and snap-to-region for known neighborhoods.

DATA STRATEGY AND OWNERSHIP
• Inventory and live rates from supplier APIs; no manual hotel entry required for launch.
• City and POI content from licensed APIs (Places), plus summarized destination cards from permissive sources cached with attribution.
• Weather from OpenWeather monthly averages + current; public holidays (Calendarific); cost-of-living seed snapshots; flights/day placeholder seeded until a live key exists.
• FX from a reliable source with 2h TTL; fallback daily table; SAR rounding rules.
• CMS-light in admin to override Arabic and English copy, featured cities, banners, and neighborhood names.
• Image pipeline: perceptual hash dedupe, EXIF strip, AVIF/WebP multipliers, deterministic filenames, CDN-ready.
• Data playbook in docs detailing source, license, TTL, caching, attribution, and how to add providers; no unlicensed scraping.

SEARCH, DISCOVERY, AND RANKING
• Query parsing with Arabic transliterations and common misspellings; synonym map for MENA cities; vector index for “similar places”.
• Ranking pipeline: eligibility → image/amenity quality → neighborhood fit → relative price vs baseline → policies (free cancel, breakfast) → diversity de-dup.
• Contextual personalization: locale, currency, month, party size only; privacy-friendly.
• Bandit experiment (ε-greedy) guarded by Remote Config for “recommended” sort; rollback in under 5 minutes.

CHECKOUT, PAYMENTS, TAX, INVOICING
• State machine PENDING → AUTHORIZED/PAID/FAILED/CANCELLED; idempotent handlers; reconciliation job for stuck webhooks.
• Coupons engine: percentage/amount, min spend, city/hotel scope, usage caps, validity windows.
• ZATCA simplified e-invoice PDF attached at confirmation; QR payload and VAT fields; invoice and receipt emails AR/EN.

BACKEND GATEWAY (GRAPHQL-FIRST WITH REST SHIM)
• Modules: Auth, Users, Search, Hotels, Bookings, Payments, Suppliers, Pricing, FX, CityIntel, Alerts, Admin, Health.
• GraphQL schema with queries and mutations: searchHotels, hotel, createBooking, confirmPayment, cancelBooking, createAlert, updateProfile, featureCities, toggleTenantFlag.
• Supplier adapters: RateHawk live v1 (search/details/booking), Hotelbeds v2 scaffold; adapter interface with retries/backoff and circuit breakers.
• Pricing engine: market/channel markup matrices, floor/ceiling guards, SAR rounding modes, length-of-stay adjustments.
• Read-models: SRP slices, PDP fragments, CityIntel cache, FX snapshots; request coalescing.
• Observability: request IDs, traces, RED metrics, histogram buckets for SRP latency; dashboards JSON and alerts.
• Health endpoints: per adapter; brownout mode with graceful degraded UX.

ADMIN AND OPERATIONS
• Admin panel: bookings lifecycle, refunds, webhook logs with retry, supplier health, featured cities, banners, coupons, CMS overrides, FX refresh, CityIntel refresh, audit logs.
• Impersonation safe mode for support with read-only session and audit trails.
• Incident playbooks: supplier outage, webhook lag, payment declines spike, map key failure; one-click rollback toggles.
• ADR process enforced on major changes; PR templates include accessibility and security checklists.

ANALYTICS, EXPERIMENTS, AND GROWTH
• Firebase Analytics + GTM; Sentry crashes.
• Event grammar with strict schemas: search_initiated, search_results_viewed, hotel_viewed, room_selected, checkout_started, payment_succeeded, payment_failed, booking_cancelled, alert_created, notification_opened, screen_view, coupon_applied, installment_opted.
• Deep link attribution: UTM captured server-side and passed to GraphQL context; campaign dashboards per tenant.
• Ratings prompt gated by NPS micro-survey; only prompt promoters.
• Shareable destination cards (server-rendered image) for social.

PERFORMANCE BUDGETS AND DEVICE MATRIX
• iOS cold start ≤ 2.3 s on iPhone 12 class; Android ≤ 2.6 s on Pixel 5 class.
• SRP TTI ≤ 1.1 s after network; PDP above-the-fold ≤ 800 ms.
• Map interactions at 60 fps with 200+ clustered markers.
• Crash-free sessions ≥ 99.6% on beta; UI jank budgets enforced.
• Testing device matrix: iOS 16–18 on A14–A17; Android 8–15 on Snapdragon 7xx/8xx and mid-range MediaTek.

ACCESSIBILITY AND ARABIC QUALITY
• VoiceOver/TalkBack labels for all actionable elements; focus order; large text snapshots.
• Arabic typography and punctuation spacing rules; Arabic-Indic numerals; mirrored layouts; Hijri date tagging where shown.
• RTL animation awareness (e.g., slide directions, drawers, map controls) and bidirectional text sanitization.

SECURITY AND PRIVACY
• TLS everywhere; certificate pinning; backup pins and rotation policy.
• Keychain/Keystore for tokens; session risk flagging; rate limits for OTP and payment attempts.
• PDPL checklists in docs; consent logs; data export/delete flows; retention periods by data category.
• PII redaction at logger; privacy budgets for analytics; IP/device velocity and BIN anomaly checks.
• MASVS mapping doc; jailbreak/root soft-block with server flags.

OFFLINE, CACHING, AND BACKGROUND
• TTL: CityIntel 24h; FX 2h; SRP 30m; PDP 15m; invalidate on booking.
• Background refresh on Wi-Fi + charging; OS quotas respected; user-visible “Last updated” and “Refresh” controls.
• Mutation queue for alerts, profile, and wishlists; payment flows require live connectivity with clear UX.

DEVELOPER EXPERIENCE AND QUALITY
• Dev Menu (shake): env switch, flags, cache inspector, simulate poor network, push tester, deep-link tester, crash simulate, show last trace ID.
• One-shot bootstrap script: installs toolchains, generates clients, seeds DB, starts gateway; if secrets missing, MOCK MODE enabled and apps display “sandbox” badge.
• Lint/format: SwiftFormat, ktlint, detekt, ESLint, Prettier; commit hooks; CODEOWNERS.
• Reproducible builds: lockfiles, pinned toolchains; checksum verification; SBOMs generated.
• Danger rules: block merges missing tests for state machines, payment handlers, and adapters.

CI/CD, RELEASE TRAINS, AND STORES
• GitHub Actions matrices for apps and gateway; cache SPM/Gradle/Node; parallelize unit/UI/contract tests; upload dSYMs and ProGuard mappings to Sentry.
• Fastlane lanes: build, test, beta, release, screenshots (AR/EN, dark/light), bump, changelog from conventional commits.
• Release trains weekly; hotfix lane; canary cohorts via Remote Config; automatic rollback when error budgets are breached.
• Store assets per tenant/country with localized keywords for KSA/GCC; privacy labels drafts included.

LEGAL AND KSA FIT
• VAT in SAR throughout; ZATCA invoice PDF with QR payload fields.
• ToS/Privacy AR/EN; quiet hours for notifications in KSA; religious holidays respected for promo scheduling.
• Age gates for promos that require it; marketing consent storage.

BACKUP, DR, AND SRE
• Nightly encrypted DB backups; seven-day retention; restore playbook; dry-run restoration CI job.
• SLOs: gateway availability 99.9%, SRP p95 < 600 ms, payment success > 96% sandbox, webhook reconciliation p95 < 5 min.
• Synthetic monitors: nightly end-to-end booking on mock and sandbox; alert to on-call.

ROADMAP STUBS (WIRED, BEHIND FLAGS)
• Flights (NDC/Amadeus), Activities (Viator/Rezdy), Cars (CarTrawler) with unified adapter interface and mocks.
• Loyalty (tiers, points, wallet) domain and read-models; accrual rules tied to bookings; reward balance in settings.
• Corporate/B2B: negotiated rates lists, SSO stub, cost centers, invoice routing.

DELIVERABLES
• iOS and Android native apps, GraphQL gateway, admin panel, read-models, seeds, generated Swift/Kotlin clients, CI/CD, Fastlane, dashboards JSON, store metadata AR/EN, design tokens, tests (unit, UI, contract, chaos), docs (architecture, ADRs, data playbook, MASVS, PDPL, runbooks), synthetic monitors, mock mode, acceptance tests.
• .env.example for TAP_, RATEHAWK_, GOOGLE_MAPS_KEY, OPENWEATHER_KEY, CALENDARIFIC_KEY, FIREBASE_*, SENTRY_DSN, DATABASE_URL; mock mode if missing.
• Replit files to start gateway/admin and build Android with a single “Run”.

ACCEPTANCE CRITERIA
• Install both apps; Arabic default with Arabic-Indic numerals; OTP login; biometrics enabled.
• Search a city; SRP list appears fast; open map; use isolines filter; open PDP; select room; pay via Tap sandbox (Apple Pay or mada or card); pass 3DS if required; see confirmation; receive push; add Wallet pass; download ZATCA PDF; booking viewable offline.
• Create a Price Watch rule; receive realtime alert on mock delta; deep link opens filtered SRP.
• Admin can switch tenant theme, set featured cities, create coupons, view supplier health and webhook lag, refresh FX/CityIntel, and see audit logs.
• CI is green; Sentry releases uploaded; dashboards show traffic, latency, errors; synthetic monitor succeeds nightly.

NOW GENERATE EVERYTHING
• Produce the complete monorepo with source code, configs, seeds, admin UI, GraphQL schema and clients, CI pipelines, Fastlane lanes, docs, dashboards, store packs, mock mode, and acceptance tests.
• Choose sensible defaults without asking questions, ensure Google Maps only, Arabic first, SAR everywhere, robust payment and booking state machines, and deliver production-grade quality end-to-end.