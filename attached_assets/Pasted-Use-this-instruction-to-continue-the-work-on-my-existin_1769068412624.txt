Use this instruction to continue the work on my existing YoRight project. Do not create a new project and do not change any UI design, colors, or layout. Only connect my existing hotel search and booking flows to the RateHawk sandbox API.
	1.	RateHawk sandbox credentials

	•	I already received a sandbox API key from RateHawk:
	•	API key ID (username): 178
	•	API key token (password): e9f483d9-2909-4245-86ba-18c3cc751dba
	•	In the backend, configure HTTP Basic Authentication for all RateHawk API calls, where:
	•	basic auth username = 178
	•	basic auth password = e9f483d9-2909-4245-86ba-18c3cc751dba
	•	Do not hardcode these values directly in the code. Store them in Replit Secrets as environment variables, for example:
	•	RATEHAWK_API_ID = 178
	•	RATEHAWK_API_TOKEN = e9f483d9-2909-4245-86ba-18c3cc751dba
Then read them from the environment in the backend.

	2.	Use sandbox environment only

	•	Use only the RateHawk sandbox endpoints and base URL (api-sandbox) from the official RateHawk documentation.
	•	Do not call any production endpoints yet.
	•	Important: use only the RateHawk sandbox URLs and keys, no production.

	3.	Hotel search and results

	•	Replace all fake or hardcoded hotel and room data in my app with real data from RateHawk sandbox.
	•	When a user searches for a city, dates, and guests in YoRight, call the correct RateHawk sandbox search / availability methods and map the response into my existing hotel cards and room cards.
	•	Keep all existing components and styling exactly as they are. Only change the data source from fake data to RateHawk sandbox data.

	4.	Booking flow and status handling
Implement the booking flow according to RateHawk’s recommendations:

	•	Use the correct sequence:
search → prebooking → booking → check booking process (and optionally webhook).
	•	Do not use the “Retrieve bookings” method during the booking process.
	•	“Retrieve bookings” must only be used to fetch booking information after the booking is already completed (for example on a “My bookings” page).
	•	Do not rely on “Retrieve bookings” to confirm that a booking succeeded.
	•	For the final booking status, implement:
	•	The “Check booking process” call, and
	•	A booking status webhook handler if possible in this project.
	•	In the booking flow, treat a booking as successful only when:
	•	the “Check booking process” response has status = “ok”, or
	•	the booking status webhook sends status = “completed”.
	•	If any other status or error happens, show a clear error state to the user and do not show the booking as confirmed.

	5.	Webhook endpoint for booking status (if possible)

	•	If technically possible in this Replit setup, create a secure webhook endpoint in the backend to receive booking status updates from RateHawk sandbox.
	•	Validate the incoming webhook, store the booking status (for example in a database or in-memory store), and expose this status to the frontend so the user can see the real final booking state.

	6.	RateHawk API client and code structure

	•	Create a dedicated RateHawk API client module that wraps all RateHawk calls in one place:
	•	search
	•	prebooking
	•	booking
	•	check booking process
	•	retrieve bookings (used only after booking is completed)
	•	Add basic logging for outbound requests and responses (without logging secrets) to help with debugging in the sandbox.
	•	Make sure timeouts, retries, and error handling follow RateHawk sandbox best practices from their documentation.

	7.	Do not touch unrelated parts of the app

	•	Do not modify branding, colors, layout, or navigation.
	•	Do not remove or rename existing pages or UI components.
	•	Only add or change the backend and integration logic needed to make YoRight use RateHawk sandbox for hotels and bookings in a stable way, ready for the certification stage later.

	8.	Use the official RateHawk documents as the source of truth
Use these documents to implement everything correctly:

API Documentation:
https://docs.emergingtravel.com

API Documentation for Sandbox:
https://docs.emergingtravel.com/docs/sandbox/

Integration and Certification Guidelines:
https://docs.emergingtravel.com/docs/sandbox/sandbox-integration-guide/

Sandbox Best Practices:
https://docs.emergingtravel.com/docs/sandbox/sandbox-best-practices-for-apiv3/

Pre-Certification Checklist:
https://docs.google.com/document/d/1jCsIKai6qd7ze-TyKh4hPV8EUpzGjjwuh3MvC9_163c/edit?tab=t.0

After you implement this, summarize for me in simple English:
	•	which RateHawk endpoints you connected,
	•	how the booking status is determined (which responses / webhook),
	•	and where I can change any configuration (like environment variables) if needed.