You are a principal-level native mobile architect and delivery lead. Build a world-class, fully NATIVE OTA called “YoRight” (Arabic default, English optional) comparable to Almosafer/Trip.com. Do NOT ask questions. Choose sane defaults. Ship working code, tests, CI/CD, seeds, docs, dashboards, and store assets. Google Maps ONLY. Saudi market first: SAR, VAT, Apple Pay, mada. Monorepo must run locally and on Replit (gateway + Android on Replit; iOS requires macOS runner).

SYSTEM OUTPUT
	•	Produce a single monorepo with:
	•	apps/ios (Swift 5.10+, SwiftUI, Combine, iOS 16+; SPM features)
	•	apps/android (Kotlin 2.0+, Jetpack Compose, Hilt, Coroutines/Flows, Android 8.0+)
	•	apps/gateway (Node 20, NestJS + Fastify, PostgreSQL, OpenAPI 3.1, Prisma or TypeORM)
	•	packages/contracts (OpenAPI spec + generated Swift/Kotlin SDKs)
	•	packages/design-tokens (JSON tokens → Swift/Kotlin generators)
	•	packages/test-fixtures (seed data and mock payloads)
	•	tools/fastlane (shared lanes for iOS/Android)
	•	tools/scripts (bootstrap, seed, smoke, screenshot, changelog)
	•	docs/ (architecture.md, sequences.md, data-playbook.md, release-runbook.md, privacy-policy-draft.md, terms-draft.md, security.md, masvs-checklist.md)
	•	store/ (App Store + Play metadata AR/EN, keywords, privacy labels, screenshot templates)

NATIVE APP FEATURES (NEW + COMPLETE)
	•	Onboarding: language (ar/en), phone/email OTP, Apple Sign-In (iOS), Google Sign-In (Android), biometric opt-in, marketing consent, VAT notice.
	•	Home: native search (city/date/guests), “Explore by month” (City Intelligence), recent searches, personalized quick chips.
	•	SRP (Hotel Search): infinite list, server-driven filters (price SAR, stars, rating, free cancel, breakfast, property type, neighborhoods, distance), sort (recommended, price, rating, distance), map toggle with clustering and draggable radius & polygon neighborhoods, prefetch PDP for top results.
	•	PDP (Hotel Details): gallery with pinch-zoom, amenities, map with pin & nearby POIs, dynamic room cards with live pricing/policies, refundable badges, reviews placeholder with seed.
	•	Checkout: guest form, upsells (breakfast/late checkout), coupon engine, VAT breakdown, totals in SAR with ≈ USD/EUR, Tap native sheet (Apple Pay/mada/cards), 3-DS, rock-solid state machine (PENDING → AUTHORIZED/PAID/FAILED/CANCELLED).
	•	Confirmation: share booking, Apple/Google Wallet pass, PDF invoice (ZATCA fields + QR payload), push confirmation, offline view, cancel/modify if policy allows.
	•	My Trips: upcoming/past, receipts, 1-tap re-book, price-drop alerts per trip.
	•	Price Alerts: rules by city/date range/hotel/threshold/currency; server evaluation + push topics.
	•	City Intelligence: monthly weather averages, public holidays, cost-of-living snapshot, “flights per day” placeholder; cached and offline-readable.
	•	Settings: profile, biometrics toggle, currency, language, notifications prefs, legal (ToS/Privacy AR/EN), data export/delete, diagnostics upload.

NATIVE-FIRST REQUIREMENTS
	•	Offline-first: normalized caches; stale-while-revalidate; queued mutations for non-financial actions; conflict policy (server timestamp LWW); background refresh on Wi-Fi + charging.
	•	iOS: Core Data (+ small Proto for compact caches).
	•	Android: Room + Proto DataStore for prefs/lookups.
	•	Maps: Google Maps SDK iOS/Android; Places Autocomplete; cluster markers; draggable radius filter; neighborhood polygons; price badges on pins.
	•	Deep links: scheme yrt:// and universal links; routes to SRP/PDP/Booking/Promo; provide QR samples for testing.
	•	Push notifications: FCM (APNs via FCM on iOS); topics price_alerts, booking_updates, refunds, promos; quiet hours for KSA.
	•	Security: TLS + certificate pinning; iOS Keychain/Android Keystore; jailbreak/root heuristics; tamper flags; device binding toggle for high-risk actions.
	•	Localization/RTL: Arabic default with Arabic-Indic numerals; English optional; perfect RTL layouts, typography, and animations; Asia/Riyadh timezone everywhere.
	•	Accessibility: VoiceOver/TalkBack, dynamic type, high contrast, focus order; large-text screenshots in tests.
	•	Animations: 60fps transitions, shimmer skeletons, haptics on pull-to-refresh and key actions.
	•	Error catalog → localized friendly titles/actions; correlation ID surfaced to user.

SUPPLIERS, DATA & CONTENT (MANDATORY)
	•	Hotels inventory/pricing/booking: RateHawk v1 LIVE adapter now; Hotelbeds v2 adapter scaffold (switchable). DTO mappers with retries/backoff and circuit breakers. Idempotent booking creation.
	•	Payments: Tap Payments intents server-side; Apple Pay/mada/cards; 3-DS; signed webhooks; idempotency keys; refunds/voids; reconciliation job for stuck webhooks.
	•	City/POI content: Google Places Details/Photos (licensed, cached with attribution), Wikivoyage/Wikipedia summaries (cached + attribution), OpenStreetMap polygons (neighborhoods).
	•	Weather: OpenWeather monthly averages + current; TTL 24h; fallback seeded norms if key missing.
	•	Public holidays: Calendarific; yearly cache per country; fallback seeded JSON.
	•	Flights/day: Aviationstack placeholder now; seeded counts per city until real key is set.
	•	FX: ECB (or similar) live every 2h; fallback daily table; rounding rules for SAR.
	•	Images: supplier photos + optional Unsplash/Pexels (licensed); EXIF strip; WebP/AVIF variants; deterministic filenames; lazy loading; CDN-ready.
	•	Seeds included: cities (Riyadh, Jeddah, Dubai, London, Paris, Bangkok, etc.), hotels with static fallback images, FX rates, monthly weather norms, public holidays sample, COL basket, flights/day placeholder, coupons, admin user.
	•	Data ownership & licensing: docs/data-playbook.md explains source, license, TTL, caching, attribution; no scraping without license; CMS-light for manual overrides.

BACKEND GATEWAY (NestJS + PostgreSQL)
	•	Modules: Auth, Users, Hotels, Bookings, Payments, Suppliers, FX, CityIntel, Alerts, Admin, Health.
	•	OpenAPI 3.1 spec in packages/contracts; generate Swift & Kotlin clients on build.
	•	Auth: email/phone OTP; short-lived JWT + refresh rotation; optional Redis for revocation.
	•	Pricing: market/channel markup matrix; VAT application; coupon engine (%/amount, min spend, scope city/hotel/date, usage caps).
	•	Observability: pino logs; request IDs; OpenTelemetry traces; metrics endpoints; dashboards JSON for Grafana/Prometheus.
	•	Health & resilience: /health per adapter; timeouts, retries, circuit breakers; brownout mode with graceful degraded UX if supplier down.
	•	Admin UI (basic): bookings table, webhook logs, supplier health, featured cities/banners, FX & CityIntel cache refresh, content overrides, coupon CRUD.

DATA MODEL (HIGHLIGHTS)
	•	User(id, email, phone, name, locale, currency, biometricAllowed, marketingConsent, createdAt)
	•	Booking(id, reference, userId?, status, supplier, supplierRef, checkIn, checkOut, cityId, hotelId, roomJson, guestJson, totals, currency, vat, createdAt, paidAt?)
	•	Payment(id, bookingId, provider, providerIntentId, status, amount, currency, rawJson, createdAt)
	•	City(id, nameAr, nameEn, countryCode, lat, lng, slug)
	•	FxRate(base, quote, rate, asOf)
	•	CityIntelCache(cityId, month, weatherJson, holidaysJson, colIndexJson, flightsJson, refreshedAt)
	•	AlertRule(userId, cityId?, hotelId?, dateRange, thresholdAmount, currency, active)

RANKING & PERSONALIZATION
	•	Ranking pipeline: availability → image/amenity score → location score → relative price competitiveness → business rules/promotions → diversity de-dup.
	•	Lightweight bandit (e-greedy) gated by Remote Config; cold-start by city priors + cohort (locale/currency/month).

PERFORMANCE BUDGETS
	•	iOS cold start ≤ 2.5s (iPhone 12 class).
	•	Android cold start ≤ 2.8s (Pixel 5 class).
	•	SRP TTI ≤ 1.2s after network on mid-tier.
	•	Maps ≥ 60fps with ≥ 200 markers clustered.
	•	Crash-free sessions ≥ 99.5% on beta.

ANALYTICS & CRASH
	•	Firebase + GTM containers; Sentry for crashes.
	•	Event taxonomy: search_initiated, search_results_viewed, hotel_viewed, room_selected, checkout_started, payment_succeeded, payment_failed, booking_cancelled, alert_created, notification_opened, screen_view. Include cityId, dates, guests, currency, stars, refundable, price.

TEST STRATEGY
	•	Unit: XCTest/JUnit for reducers, mappers, pricing, webhook handlers.
	•	UI: XCUITest/Espresso for onboarding, search, PDP, checkout, confirmation, offline view.
	•	Contract: Prism mock server for OpenAPI; run in CI.
	•	E2E smoke (nightly): full happy-path booking against sandbox with mock supplier.
	•	Performance: cold start + SRP render micro-benchmarks in CI.

CI/CD & RELEASE
	•	GitHub Actions: matrix builds; cache SPM/Gradle; upload dSYMs/ProGuard to Sentry; artifact retention.
	•	Fastlane lanes: build, test, beta, release, bump, screenshots, changelog from conventional commits.
	•	Versioning: semver + synchronized marketing/build numbers.
	•	Distribution: TestFlight internal/external; Play Internal/Closed tracks.
	•	Release trains: weekly schedule; hotfix lane; canary cohorts via Remote Config.

STORE & BRAND
	•	Luxury travel vibe; generous spacing; rounded-2xl; subtle shadows; dark/light; high-contrast variant.
	•	Tokens in JSON → platform themes.
	•	store/metadata: AR/EN titles, subtitles, long/short descriptions, keywords (KSA/GCC), privacy labels, screenshot scripts (Fastlane) generating both languages and modes.

SECURITY & COMPLIANCE (KSA)
	•	MASVS mapping in docs/masvs-checklist.md; implemented items noted.
	•	PII minimization; encryption at rest; structured retention; data export/delete flows.
	•	ZATCA simplified e-invoice PDF (QR payload fields) attached to confirmation; VAT breakdown in SAR.
	•	Age gates for promos; marketing consent storage; device & IP-based rate-limits for OTP and payments.

DEVELOPER EXPERIENCE
	•	One-shot scripts:
	•	scripts/bootstrap: install deps, generate SDKs, seed DB, start gateway; if keys missing, auto-mock with realistic payloads so the demo runs end-to-end.
	•	scripts/smoke: run SRP→PDP→checkout against sandbox or mock, assert success.
	•	scripts/screenshot: run automated UI to capture AR/EN screenshots.
	•	Dev Menu (shake): env switch, feature flags, cache inspector, simulate poor network, trigger test push, deep-link tester, crash simulate.

BOOTSTRAP & CONFIG
	•	.env.example includes TAP_, RATEHAWK_, GOOGLE_MAPS_KEY, OPENWEATHER_KEY, CALENDARIFIC_KEY, FIREBASE_*, SENTRY_DSN, DATABASE_URL.
	•	If a key is absent, gateway enters MOCK MODE with realistic responses; apps show a “sandbox” badge.
	•	seeds run automatically on first start; admin user and coupons ready.

ACCEPTANCE CRITERIA
	•	Install both apps; complete onboarding in Arabic (RTL, Arabic-Indic numerals); OTP login; enable biometrics.
	•	Search “Dubai” with dates; see SRP list; open map; filter by price/stars; open PDP; select room; pay via Tap sandbox (Apple Pay or card); pass 3-DS if prompted; see confirmation; receive push; add to Wallet; download PDF invoice; then open booking offline (airplane mode).
	•	Create price alert for Riyadh; receive push when mock threshold triggers; deep link opens SRP.
	•	Admin shows booking lifecycle + webhook logs; can refresh FX & CityIntel caches; edit featured cities; create coupon; see supplier health.
	•	Analytics events visible in DebugView; Sentry release symbols uploaded; CI green on unit/UI/contract tests.

NON-NEGOTIABLES
	•	Do not reuse any web app scaffolds; build genuine native codebases.
	•	Google Maps is the only maps provider.
	•	Arabic is the default locale; all dates/time localized to Asia/Riyadh.
	•	Payment flows must never start offline; show clear messaging when connectivity is required.
	•	All secrets externalized; never commit keys.
	•	Include unit tests for supplier→internal DTO mappers, booking/payment state machine, Tap webhook handler.
	•	Include basic cold-start and SRP rendering benchmarks.
	•	Include dashboards JSON and step-by-step runbooks.

DELIVER NOW
	•	Full iOS and Android native projects; NestJS gateway; OpenAPI spec & generated clients; seeds & fixtures; CI pipelines; Fastlane lanes; store metadata drafts; design tokens; tests; docs; dashboards; scripts; mock mode; admin UI; acceptance tests; and a README explaining how to add Flights, Activities, and Cars later with the same adapter pattern.

DATA OWNERSHIP & TEAM WORKFLOW (WHO PREPARES THE DATA?)
	•	Phase 1: suppliers + APIs supply hotels, images, policies, and live prices. No manual hotel entry required.
	•	Phase 2: light Content Ops (1-2 people) manage Arabic destination copy, featured cities, banners, and overrides from /admin.
	•	All external data licensed or used via APIs with attribution; no unlicensed scraping. The data-playbook documents each source, TTLs, and usage rights.