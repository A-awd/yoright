I already have an existing project here for my travel brand “YoRight”.
All UI and design screens are ready. Do NOT change the design, layout, colors, components, or navigation.
Your job is only backend logic and data connections.

I am not a developer. Act as my senior full-stack engineer and system architect. Make your own good decisions and do not ask me many questions. After each big step, explain to me in simple English what you did.

Goal: prepare this project so it is fully ready to connect to RateHawk B2B API v3 as soon as I receive my Sandbox API key.

Use these docs only for the structure (I do not have the key yet):
	•	Main docs: https://docs.emergingtravel.com/
	•	Best Practices: https://docs.emergingtravel.com/docs/best-practices-for-apiv3/
	•	Integration & Certification Guide: https://docs.emergingtravel.com/docs/integration-guide/

	1.	Configuration (placeholders only)
Create a secure config/environment setup in this project with placeholders like:

	•	RATEHAWK_API_KEY = “TO_BE_ADDED_LATER”
	•	RATEHAWK_PARTNER_ID = “TO_BE_ADDED_LATER”
Write all backend code so that when I later paste the real Sandbox key and partner id in these variables, everything starts working without big changes.
All future calls to RateHawk must be from the backend only, never directly from the browser.

	2.	Backend structure that matches my existing UI
In this YoRight project I already have:

	•	A search form screen (destination + dates + guests)
	•	A search results screen (list of hotels)
	•	A hotel details screen
	•	A booking/checkout screen and a confirmation screen

Without changing the visual design, do the following:

a) Search endpoint and wiring
	•	Create a backend endpoint, for example: GET /api/hotels/search
	•	Inputs: city/destination, check-in date, check-out date, number of adults, number of children, currency.
	•	For now, use mocked data but shape it like the RateHawk SERP response: hotel id, hotel name, city, star rating, main photo URL, lowest price per night, total price for stay, refundable/non-refundable flag.
	•	Bind this endpoint to the existing search form and results screen so that the hotels list is dynamic and driven by this endpoint.

b) Hotel details endpoint and wiring
	•	Create a backend endpoint, for example: GET /api/hotels/{hotelId}
	•	For now, return mocked data shaped like the RateHawk “hotelpage” response: photos, description, address, map coordinates, room types, prices, meal plan, cancellation rules.
	•	Bind this data to my existing hotel details screen so that it shows dynamic information based on the selected hotel from the results.

c) Booking endpoint and wiring
	•	On the existing booking/checkout screen, connect the “Book” button to a backend endpoint, for example: POST /api/bookings
	•	Input: hotel id, selected room/rate info, guest names, contact details, stay dates.
	•	For now, simulate the RateHawk flow (prebook → book) with mocked responses.
	•	Return a clear JSON result: booking status, fake booking reference, total price, main guest name.
	•	Bind this result to my existing confirmation screen so that after pressing “Book”, the user sees a confirmation with these details.

	3.	Easy switch from mock to real RateHawk later
Design the code so that later, when I receive the real RateHawk Sandbox API key and partner id, we only need to:

	•	Paste them into the environment variables, and
	•	Replace the mocked data in these endpoints with real calls to the correct RateHawk methods (SERP, hotelpage, prebook, book),
without changing anything in the UI screens.

	4.	Follow the spirit of RateHawk best practices
While designing the flow, keep the recommended sequence from the docs: search → details → prebook → book → confirmation, and structure the data models accordingly.
Prepare the project so that later I can easily have one staging URL where RateHawk can test: search, open hotel details, and make a test booking.

When you finish each major part (search, details, booking), write a short summary in simple English telling me what is working now and how it is connected inside this existing YoRight project.